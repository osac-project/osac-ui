{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://osac.internal/specs/ui-flow.schema.json",
  "title": "OSAC UI Flow Spec",
  "description": "Formal schema for ui-flows/*.yaml files — describes user-visible journeys, step states, transitions, RBAC gates, and validation rules for the OSAC demo application.",
  "type": "object",

  "required": ["flow", "statechart_ref", "rbac", "steps", "transitions", "validation_rules"],

  "properties": {
    "flow": {
      "type": "string",
      "description": "Human-readable name for this flow, matching the title in FLOW-INDEX.yaml flows[].",
      "minLength": 1
    },

    "statechart_ref": {
      "type": "string",
      "description": "Relative path to the companion statechart YAML file.",
      "pattern": "^\\.\\./statecharts/.+\\.statechart\\.yaml$"
    },

    "rbac": {
      "type": "object",
      "description": "RBAC gates for each step in this flow.",
      "required": ["required_permissions_by_step"],
      "properties": {
        "required_permissions_by_step": {
          "type": "object",
          "description": "Map of canonical step ID → map of action label → required permission string.",
          "additionalProperties": {
            "type": "object",
            "description": "Map of action label → permission name.",
            "additionalProperties": { "type": "string" }
          }
        }
      },
      "additionalProperties": false
    },

    "steps": {
      "type": "array",
      "description": "Ordered list of UX steps (screens, modals, or logical waypoints) in this flow.",
      "minItems": 1,
      "items": { "$ref": "#/$defs/Step" }
    },

    "transitions": {
      "type": "array",
      "description": "Directed edges describing how the user or system moves between steps across flows.",
      "items": { "$ref": "#/$defs/Transition" }
    },

    "validation_rules": {
      "type": "array",
      "description": "Client-side field validation constraints for this flow. Empty array is valid when there are no constraints.",
      "items": { "$ref": "#/$defs/ValidationRule" }
    },

    "applies_when": {
      "type": "string",
      "description": "Optional prose describing the RBAC / session conditions under which this flow is active (e.g. role and tenant context)."
    },

    "terminology": {
      "type": "object",
      "description": "Optional glossary of domain terms, display-name clarifications, or structured content tables specific to this flow. Values may be strings (simple glossary entries) or structured objects/arrays (e.g. os_families table).",
      "additionalProperties": true
    },

    "entry_bootstrap": {
      "$ref": "#/$defs/EntryBootstrap"
    },

    "routing_matrix": {
      "type": "array",
      "description": "Optional list of role/persona routing choices available on the welcome landing. Used only in welcome-and-role-selection.",
      "items": { "$ref": "#/$defs/RoutingChoice" }
    },

    "sign_in_shell": {
      "$ref": "#/$defs/SignInShell"
    },

    "branding_profiles": {
      "type": "array",
      "description": "Optional per-tenant branding for institutional-sign-in when using sign_in_shell.",
      "items": { "$ref": "#/$defs/BrandingProfile" }
    },

    "pre_login_document_titles": {
      "type": "object",
      "description": "Optional mapping of tenant context to document.title values shown before authentication.",
      "required": ["description", "titles"],
      "properties": {
        "description": { "type": "string" },
        "titles": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["tenant_context", "title"],
            "properties": {
              "tenant_context": { "type": "string" },
              "title": { "type": "string" }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },

    "pre_login_theme_defaults": {
      "type": "object",
      "description": "Optional per-tenant dark/light theme applied before sign-in.",
      "required": ["description", "dark_theme_tenants", "light_theme_tenants"],
      "properties": {
        "description": { "type": "string" },
        "dark_theme_tenants": {
          "type": "array",
          "items": { "type": "string" }
        },
        "light_theme_tenants": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "additionalProperties": false
    },

    "role_preconditions": {
      "type": "object",
      "description": "Optional map describing which branding profile and pre-filled identifier apply per persona after welcome routing. May include a top-level 'description' string.",
      "properties": {
        "description": {
          "type": "string",
          "description": "Optional prose overview of this section."
        }
      },
      "additionalProperties": {
        "oneOf": [
          { "type": "string" },
          {
            "type": "object",
            "properties": {
              "role": { "type": "string" },
              "tenant_id": { "type": "string" },
              "tenant_ids": { "type": "array", "items": { "type": "string" } },
              "entry": { "type": "string" },
              "prefill": { "type": "string" }
            },
            "additionalProperties": false
          }
        ]
      }
    }
  },

  "additionalProperties": false,

  "$defs": {
    "Step": {
      "type": "object",
      "description": "A single UX waypoint — a screen, modal, drawer, or logical state within the flow.",
      "required": ["id", "title", "goal", "ui", "states", "interactions"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Globally unique canonical step ID (see FLOW-INDEX canonical_step_ids). Prefix matches the flow (e.g. shell_, auth_, tud_, mvm_).",
          "pattern": "^[a-z][a-z0-9_]*$"
        },

        "title": {
          "type": "string",
          "description": "Short human-readable label for this step.",
          "minLength": 1
        },

        "goal": {
          "type": "string",
          "description": "One-sentence description of what the user achieves in this step.",
          "minLength": 1
        },

        "ui": {
          "type": "object",
          "description": "PatternFly / logical component names rendered in this step.",
          "required": ["components"],
          "properties": {
            "components": {
              "type": "array",
              "description": "List of logical component names from FLOW-INDEX reusable_components_across_flows or PF primitives used directly.",
              "minItems": 1,
              "items": { "type": "string", "minLength": 1 }
            },
            "notes": {
              "type": "array",
              "description": "Optional prose notes for implementers describing design intent, icon usage, or composition patterns not expressible in the components list.",
              "items": { "type": "string", "minLength": 1 }
            }
          },
          "additionalProperties": false
        },

        "states": {
          "type": "array",
          "description": "Discrete UI states this step can be in (mirrors statechart states).",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },

        "interactions": {
          "type": "array",
          "description": "Human-readable list of user actions or system behaviours available in this step.",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },

        "role_variants": {
          "type": "object",
          "description": "Optional per-role presentation differences (sidebar nav, masthead, fleet scope). Used in application-shell-session.",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "sidebar_nav": { "type": "string" },
              "masthead": { "type": "string" },
              "fleet_scope": { "type": "string" }
            },
            "additionalProperties": false
          }
        },

        "content": {
          "$ref": "#/$defs/StepContent"
        },

        "entry_context": {
          "type": "object",
          "description": "Optional map of named entry paths into this step with a prose description of each. Used in institutional-sign-in.",
          "additionalProperties": {
            "type": "object",
            "required": ["description"],
            "properties": {
              "description": { "type": "string" }
            },
            "additionalProperties": false
          }
        },

        "ui_policy": {
          "$ref": "#/$defs/UiPolicy"
        }
      },
      "additionalProperties": false
    },

    "UiPolicy": {
      "type": "object",
      "description": "Optional implementation policy for UI composition (e.g. prefer PatternFly primitives over raw wrappers).",
      "properties": {
        "prefer_patternfly_primitives": {
          "type": "boolean",
          "description": "When true, prefer PatternFly layout/composition components for structure."
        },
        "minimize_raw_div_wrappers": {
          "type": "boolean",
          "description": "When true, avoid structural raw div containers unless no practical PF equivalent exists."
        },
        "max_raw_div_wrappers": {
          "type": "integer",
          "minimum": 0,
          "description": "Cap on raw opening <div> tags (without pf-primitive-exception) in TSX that implements this step. Enforced by pnpm check:pf-primitives: each file's leading block comment must include flow: <kebab-flow-id> matching docs/specs/ui-flows/<flow>.yaml, and optionally step: <id> or steps: id1, id2; otherwise the checker uses the max budget across all steps in that flow. Non-div raw tags are not covered by this number (they always require pf-primitive-exception). See design-system.yaml layout_and_shell.implementation_policy.automated_check_alignment."
        },
        "avoid_custom_css": {
          "type": "boolean",
          "description": "When true, implementations must not introduce custom CSS classes for layout or spacing. Use PF layout primitives (Grid/GridItem, Gallery/GalleryItem, Flex/FlexItem, Stack/StackItem, Split/SplitItem) and PF design tokens via inline style props instead. Only exceptions: chart SVG bounding box containers (add pf-primitive-exception comment) and global scrollport rules in global.css."
        }
      },
      "additionalProperties": false
    },

    "StepContent": {
      "type": "object",
      "description": "Optional static copy and content structure for a step (used on the welcome landing).",
      "properties": {
        "heading_h1": { "type": "string" },
        "lede": { "type": "string" },
        "demo_alert": {
          "type": "object",
          "required": ["title", "body_paragraphs"],
          "properties": {
            "title": { "type": "string" },
            "body_paragraphs": {
              "type": "array",
              "items": { "type": "string" }
            }
          },
          "additionalProperties": false
        },
        "provider_card": {
          "type": "object",
          "required": ["title", "copy", "primary_action_label"],
          "properties": {
            "title": { "type": "string" },
            "copy": { "type": "string" },
            "primary_action_label": { "type": "string" }
          },
          "additionalProperties": false
        },
        "org_card_copy": { "type": "string" },

        "role_columns": {
          "type": "object",
          "description": "Three-column role-picker layout (provider_admin, tenant_admin, tenant_user).",
          "properties": {
            "provider_admin": {
              "type": "object",
              "properties": {
                "title": { "type": "string" },
                "icon": { "type": "string" },
                "copy": { "type": "string" },
                "primary_action_label": { "type": "string" }
              },
              "additionalProperties": false
            },
            "tenant_admin": { "$ref": "#/$defs/RoleColumn" },
            "tenant_user":  { "$ref": "#/$defs/RoleColumn" }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },

    "Transition": {
      "type": "object",
      "description": "A directed edge from one step to another, triggered by an action.",
      "required": ["from", "action", "to"],
      "properties": {
        "from": {
          "type": "string",
          "description": "Canonical step ID of the source step (may be in this flow or another).",
          "minLength": 1
        },
        "action": {
          "type": "string",
          "description": "Human-readable description of the user or system event that triggers this transition.",
          "minLength": 1
        },
        "to": {
          "type": "string",
          "description": "Canonical step ID of the destination step (may be in this flow or another).",
          "minLength": 1
        },
        "browser_target": {
          "type": "string",
          "description": "Optional — whether the transition opens in the same tab or a new tab.",
          "enum": ["same_tab", "new_tab"]
        }
      },
      "additionalProperties": false
    },

    "ValidationRule": {
      "type": "object",
      "description": "Client-side validation constraint for a specific field in this flow.",
      "required": ["field", "rules"],
      "properties": {
        "field": {
          "type": "string",
          "description": "Name of the field or logical input being validated.",
          "minLength": 1
        },
        "when": {
          "type": "string",
          "description": "Optional condition under which this validation applies (e.g. 'deployment equals new')."
        },
        "rules": {
          "type": "array",
          "description": "One or more named validation constraints.",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["rule"],
            "properties": {
              "rule": {
                "type": "string",
                "description": "Machine-readable rule identifier (e.g. non_empty_trimmed_strings_required, exactly_one_choice_required).",
                "minLength": 1
              },
              "note": {
                "type": "string",
                "description": "Optional prose explaining prototype behaviour or QA expectations."
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },

    "EntryBootstrap": {
      "type": "object",
      "description": "Describes optional query-param bootstrap and related navigation rules for welcome and sign-in.",
      "required": ["description", "query_param", "allowed_values", "unknown_value_behavior"],
      "properties": {
        "description": { "type": "string" },
        "query_param": {
          "type": "string",
          "description": "URL query parameter name used to bootstrap tenant and role on page load."
        },
        "browser_history": {
          "type": "object",
          "description": "How SPA routing interacts with the browser history stack (e.g. Back after welcome → sign-in).",
          "required": ["description"],
          "properties": {
            "description": { "type": "string" }
          },
          "additionalProperties": false
        },
        "allowed_values": {
          "type": "array",
          "description": "Exhaustive list of recognised query param values.",
          "minItems": 1,
          "items": { "type": "string" }
        },
        "unknown_value_behavior": {
          "type": "string",
          "description": "Prose description of how the app behaves when an unrecognised value is provided."
        }
      },
      "additionalProperties": false
    },

    "RoutingChoice": {
      "type": "object",
      "description": "One row in the welcome routing matrix — maps a user card/button choice to browser target, tenant, role, and navigation destination.",
      "required": ["choice", "browser_target", "tenant_id", "role"],
      "properties": {
        "choice": {
          "type": "string",
          "description": "Label of the card or button the user clicks (e.g. 'Provider Admin — Enter').",
          "minLength": 1
        },
        "browser_target": {
          "type": "string",
          "enum": ["same_tab", "new_tab"]
        },
        "tenant_id": {
          "type": "string",
          "description": "Internal tenant identifier set in app state (e.g. vertexa, northstar, evergreen)."
        },
        "role": {
          "type": "string",
          "description": "Demo shell role applied (e.g. providerAdmin, tenantUser, tenantAdmin)."
        },
        "next_step": {
          "type": "string",
          "description": "Canonical step ID the user lands on in this tab after the choice."
        },
        "then": {
          "type": "string",
          "description": "Canonical step ID of the subsequent step (same tab, after next_step)."
        },
        "osac_entry": {
          "type": "string",
          "description": "Equivalent osac-entry slug for optional deep links or bookmarks; welcome card clicks use same-tab session state instead of this query param."
        }
      },
      "additionalProperties": false
    },

    "SignInShell": {
      "type": "object",
      "description": "Single React shell implementing institutional sign-in; branding_profiles supply per-tenant configuration.",
      "required": ["component"],
      "properties": {
        "description": {
          "type": "string",
          "description": "Optional prose for implementers."
        },
        "component": {
          "type": "string",
          "description": "Name of the React component implementing auth_sign_in_form for all institutions.",
          "minLength": 1
        }
      },
      "additionalProperties": false
    },

    "BrandingProfile": {
      "type": "object",
      "description": "Per-tenant branding and persona scope for the institutional sign-in shell.",
      "required": ["id", "tenant_id", "display_name", "personas"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Internal identifier for this profile (e.g. vertexa_provider).",
          "minLength": 1
        },
        "tenant_id": {
          "type": "string",
          "description": "Stable tenant key used by session routing (vertexa | northstar | evergreen).",
          "minLength": 1
        },
        "display_name": {
          "type": "string",
          "description": "Human-readable institution name shown in UI."
        },
        "personas": {
          "type": "string",
          "description": "Prose describing which demo personas use this profile."
        }
      },
      "additionalProperties": false
    },

    "RoleColumn": {
      "type": "object",
      "description": "Tenant role column on the welcome landing (tenant_admin or tenant_user).",
      "properties": {
        "title": { "type": "string" },
        "icon": { "type": "string" },
        "copy": { "type": "string" },
        "tenant_actions": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["label"],
            "properties": {
              "label": { "type": "string" }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  }
}
